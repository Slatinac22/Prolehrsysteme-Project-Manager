import 'package:flutter/material.dart';

class KolicinaPage extends StatelessWidget {
  const KolicinaPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Kolicina'),
      ),
      body: Center(
        child: Text('Placeholder for Kolicina '),
      ),
    );
  }
}