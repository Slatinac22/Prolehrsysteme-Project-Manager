import 'package:flutter/material.dart';

class MontazaPage extends StatelessWidget {
  const MontazaPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Montaza'),
      ),
      body: Center(
        child: Text('Placeholder for montaza Page'),
      ),
    );
  }
}