import 'package:flutter/material.dart';

class PlanProdukcijePage extends StatelessWidget {
  const PlanProdukcijePage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Plan Produkcije'),
      ),
      body: Center(
        child: Text('Placeholder for Plan Page'),
      ),
    );
  }
}