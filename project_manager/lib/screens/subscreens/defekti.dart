import 'package:flutter/material.dart';

class DefektiPage extends StatelessWidget {
  const DefektiPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Defekti'),
      ),
      body: Center(
        child: Text('Placeholder for Defekti Page'),
      ),
    );
  }
}