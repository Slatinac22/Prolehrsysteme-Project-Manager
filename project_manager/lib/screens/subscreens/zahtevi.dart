import 'package:flutter/material.dart';

class ZahteviPage extends StatelessWidget {
  const ZahteviPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Zahtevi'),
      ),
      body: Center(
        child: Text('Placeholder for Zahtevi Page'),
      ),
    );
  }
}