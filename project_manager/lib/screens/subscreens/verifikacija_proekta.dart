import 'package:flutter/material.dart';

class VerifikacijaProjektaPage extends StatelessWidget {
  const VerifikacijaProjektaPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Verifikacija Projekta '),
      ),
      body: Center(
        child: Text('Placeholder for VerifikacijaProjektaPage '),
      ),
    );
  }
}