import 'package:flutter/material.dart';

class SkladisteITransportPage extends StatelessWidget {
  const SkladisteITransportPage({super.key});

  @override
  Widget build(BuildContext context) {
       return Scaffold(
      appBar: AppBar(
        title: Text('Skladiste i transport'),
      ),
      body: Center(
        child: Text('Placeholder for skladiste Page'),
      ),
    );
  }
}